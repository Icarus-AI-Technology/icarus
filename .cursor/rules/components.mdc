---
description: Regras para componentes UI Dark Glass Medical
globs: ["src/components/**/*.tsx", "src/pages/**/*.tsx"]
---

# Componentes Dark Glass Medical

## Componentes Base Disponíveis

### GlassCard
```tsx
import { GlassCard } from '@/components/ui/primitives';

// Variantes de elevation: flat | raised | floating
// Variantes de glow: none | primary | success | danger
<GlassCard elevation="raised" glow="primary" interactive animated>
  <div className="p-6">Conteúdo</div>
</GlassCard>
```

### NeuButton
```tsx
import { NeuButton } from '@/components/ui/primitives';

// Variantes: default | primary | success | danger | ghost | outline
// Tamanhos: sm | md | lg | xl | icon
<NeuButton 
  variant="primary" 
  size="md" 
  loading={isLoading}
  leftIcon={<Save size={16} />}
>
  Salvar
</NeuButton>
```

### NeuInput
```tsx
import { NeuInput } from '@/components/ui/primitives';

<NeuInput
  placeholder="Digite..."
  state="default" // default | error | success
  inputSize="md"
  leftIcon={<Search size={16} />}
  error="Mensagem de erro"
/>
```

### Icon3D
```tsx
import { Icon3D } from '@/components/ui/primitives';

// Variantes: default | primary | success | warning | danger
<Icon3D 
  variant="primary" 
  size="lg" 
  icon={<Calendar size={24} />} 
/>
```

### KPICard
```tsx
import { KPICard } from '@/components/ui/data-display';

<KPICard
  title="Cirurgias Hoje"
  value={42}
  subtitle="12 confirmadas"
  trend={{ value: 15, label: "vs semana passada" }}
  icon={<Calendar size={20} />}
  accentColor="blue" // blue | green | amber | red | cyan
  loading={false}
  animated
/>
```

### Badge
```tsx
import { Badge } from '@/components/ui/data-display';

// Variantes: default | primary | success | warning | danger | info
<Badge variant="success" size="md" dot>
  Ativo
</Badge>
```

### StatusIndicator
```tsx
import { StatusIndicator } from '@/components/ui/data-display';

// Status: success | warning | error | info | pending
<StatusIndicator status="success" label="Aprovado" showIcon size="md" />
```

### AlertCard
```tsx
import { AlertCard } from '@/components/ui/data-display';

<AlertCard
  type="warning" // info | warning | error | success
  title="Estoque Baixo"
  description="Produto XYZ abaixo do mínimo"
  value="R$ 1.500,00"
  timestamp="Há 5 minutos"
  action={{ label: "Ver detalhes", onClick: () => {} }}
/>
```

### ProgressBar
```tsx
import { ProgressBar } from '@/components/ui/data-display';

<ProgressBar
  value={75}
  max={100}
  label="Progresso"
  showValue
  color="blue"
  animated
/>
```

## Padrões de Estilo Obrigatórios

### Classes Tailwind Padrão
```tsx
// Container de página
<div className="min-h-screen bg-[#050508] p-6">

// Grid de KPIs
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">

// Card interno
<div className="p-6 space-y-4">

// Texto primário
<h1 className="text-2xl font-bold text-white">

// Texto secundário
<p className="text-sm text-slate-400">

// Texto muted
<span className="text-xs text-slate-500">
```

### Animações com Framer Motion
```tsx
import { motion, AnimatePresence } from 'framer-motion';

// Fade in
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  exit={{ opacity: 0, y: -20 }}
  transition={{ duration: 0.3 }}
>

// Stagger children
<motion.div
  variants={{
    hidden: { opacity: 0 },
    show: { opacity: 1, transition: { staggerChildren: 0.1 } }
  }}
  initial="hidden"
  animate="show"
>
```

### Ícones (Lucide React)
```tsx
import { 
  Calendar, Package, AlertTriangle, DollarSign, 
  TrendingUp, TrendingDown, Search, Plus, Edit, Trash,
  Check, X, ChevronRight, Settings, User, LogOut
} from 'lucide-react';

// Tamanhos padrão
<Icon size={16} /> // small
<Icon size={20} /> // medium (default)
<Icon size={24} /> // large
```

## Estrutura de Componente

```tsx
'use client'; // Se usar hooks

import * as React from 'react';
import { motion } from 'framer-motion';
import { cn } from '@/lib/utils';
import { GlassCard } from '@/components/ui/primitives';

interface MyComponentProps {
  title: string;
  children: React.ReactNode;
  className?: string;
}

export function MyComponent({ title, children, className }: MyComponentProps) {
  return (
    <GlassCard className={cn('p-6', className)}>
      <h2 className="text-lg font-semibold text-white mb-4">{title}</h2>
      {children}
    </GlassCard>
  );
}
```

## Anti-padrões (EVITAR)

```tsx
// ❌ Cores hardcoded fora do design system
<div className="bg-gray-800">

// ✅ Usar variáveis do design system
<div className="bg-[#12121a]">

// ❌ Shadows genéricas
<div className="shadow-lg">

// ✅ Usar shadows neumórficas
<div className="shadow-[4px_4px_8px_rgba(0,0,0,0.5),-4px_-4px_8px_rgba(255,255,255,0.03)]">

// ❌ Border radius inconsistente
<div className="rounded-md">

// ✅ Usar rounded-2xl (16px) padrão
<div className="rounded-2xl">

// ❌ Importar ícones de outras libs
import { FaCalendar } from 'react-icons/fa';

// ✅ Usar Lucide React
import { Calendar } from 'lucide-react';
```
